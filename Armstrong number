#include<stdio.h>
#include<conio.h>
main()
{
int n,r,sum=0,t;
clrscr();
printf("\n enter a number");
scanf("%d",&n);
t=n;
while(n!=0)
{
r=n%10;
sum=sum+(r*r*r);
n=n/10;
}
if(sum==t)
printf("\n Armstrong number");
else
printf("\n not an Armstrong number");
getch();
}
